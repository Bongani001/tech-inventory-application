extends layout 

block content
  h1= title
  
  p List of all categories available
  hr
  if categories.length
    ul
      each category in categories
        li.m-2
          a(href=category.url)= category.name.toUpperCase()